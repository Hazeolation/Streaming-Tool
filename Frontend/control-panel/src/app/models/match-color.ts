/**
 * Defines the `MatchColor` interace that holds data on what colors exactly get used for a certain match
 */
export interface MatchColor {
  id: number;
  colorAlpha: string;
  colorBravo: string;
}
