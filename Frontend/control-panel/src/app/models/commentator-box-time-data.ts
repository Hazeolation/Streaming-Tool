/**
 * Defines the `CommentatorBoxTimeData` interface, which represents the display and hide times of the commentator box on the map screen.
 */
export interface CommentatorBoxTimeData {
  hideDisplayIntervalInSeconds: number;
}
