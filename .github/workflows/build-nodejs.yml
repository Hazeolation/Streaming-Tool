# This workflow will do a clean installation of node dependencies, and run tests.
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Build Node.js CI

env:
    FRONTEND_PATH: ./Frontend/control-panel

defaults:
    run:
        shell: bash

on:
    push:
        branches: ["master"]
    pull_request:
        branches: ["master"]

jobs:
    build:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [22.x]

        steps:
            - uses: actions/checkout@v4
            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v4
              with:
                  node-version: ${{ matrix.node-version }}
                  cache: "npm"
                  cache-dependency-path: ${{ env.FRONTEND_PATH }}/package-lock.json
            - name: Install dependencies
              run: npm ci
              working-directory: ${{ env.FRONTEND_PATH }}
            - name: Build
              run: npm run build --if-present
              working-directory: ${{ env.FRONTEND_PATH }}
            # Commented out until tests exist
            # - name: Test
            #   run: npm test
            #   working-directory: ./Frontend/control-panel
